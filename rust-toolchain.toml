# Pin the gnu HOST toolchain: it bundles MinGW binutils + system-DLL import libs,
# so the x86_64-pc-windows-gnu target (SPEC C2) links with zero external installs.
# The msvc toolchain cannot link this target (verified 2026-07-03, see SPEC §2).
[toolchain]
channel = "stable-x86_64-pc-windows-gnu"
