# Keep the compiler host-neutral. Cross compilation is selected with
# `--target`; encoding a target triple in `channel` makes rustup look for a
# non-existent release channel on macOS and Linux.
[toolchain]
channel = "1.97.1"
targets = ["x86_64-pc-windows-gnu"]
components = ["rustfmt", "clippy"]
profile = "minimal"
